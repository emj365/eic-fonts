dependencies:
  override: [npm install]
  pre: ['echo "//registry.npmjs.org/:_authToken=$NPM_TOKEN" >> ~/.npmrc']
deployment:
  npm:
    commands: [npm run build, npm publish]
    tag: /v[0-9]+(\.[0-9]+)*/
